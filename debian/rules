#!/usr/bin/make -f
%:
	dh $@ --with python3 --with systemd


override_dh_systemd_enable:
	dh_systemd_enable --name=pt-sys-menu

override_dh_installinit:
	dh_installinit --noscripts

override_dh_systemd_start:
	dh_systemd_start pt-sys-menu
